/* Ternary */

const alter = 33;
alter >= 18 ? console.log(">= 18") : console.log("< 18");

/* Als Variablen Zuweisung */

const istBrief = false;
const anrede = istBrief ? "Sehr geehrter..." : "Hallo...";
console.log(anrede);
